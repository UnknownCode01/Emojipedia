# Emojipedia
Here I made a website using react which keeps description of emoji<br>
To run the code :<br>
Open terminal -> Goto the working or current repository using cd command
type npm i
type npm start
